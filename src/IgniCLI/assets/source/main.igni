package program;

class Vector2(val x: Int, val y: Int) {
	fun printSum() {
		val sum = x + y;
        println(sum);
	}
}

fun main() {
	val arr = arrayOf(1, 2, 3, 4, 5);
	for (elem in arr) {
		println(elem);
	}

	val vec = Vector2(100, 50);

	vec.printSum();
}